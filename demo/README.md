# Demo

The demo is a small console simulation that feeds allocation, queue, dispatch,
and frame-budget samples into a debug session and prints the resulting snapshot.

Run it locally:

```bash
npm run demo
```
